# CLAUDE.md

このファイルは、このリポジトリで作業する際にClaude Code (claude.ai/code) に向けたガイドです。

## プロジェクト概要

**実在する店舗の受注案件です**(デモサイトではありません)。内容を勝手に創作しないでください。

- 店舗: 脱毛・リラク SOCU(福岡市博多区・博多駅徒歩5分)
- セラピスト: キシさん(男性1名)。全メニュー男性限定
- 主力: チネイザン(タイ古式の腹部トリートメント)。ほかにオイルトリートメント、メンズ脱毛
- 条件: 料金10万円(買い切り)／納期1週間
- 本番URL: https://socu-relaxation.com （キシさん名義のCloudflareでホスティング。
  mainへpushすると自動でデプロイされる）

## このサイトのゴール

> 博多に来た国内・海外の観光客が、スマホで見つけて「ここなら安心して入れそう」と思い、
> そのままLINEかInstagramで予約できるサイト。

判断に迷ったらこの一文に立ち返ること。詳細は `docs/サイト構成案.md` を参照。

## 必ず守ること

- **事実は `docs/` が正**。料金・住所・営業時間は `docs/メニュー・料金.md` と `docs/店舗情報まとめ.md` から取る
- **電話番号は載せない**。電話予約を受け付けていないため(意図的な仕様)
- **お客様の声は実在のGoogleクチコミ**。作文して増やさない
- **全メニューが男性限定**。女性向けの表現を入れない
- 日本語と英語は必ずセットで直す(片方だけ直すと表示が食い違う)

## 構成

素のHTML/CSS + 小さなインラインJSのみ。ビルドシステム・パッケージマネージャー・テストはありません。

- `index.html` … 1ページ完結のランディングページ
- `styles.css` … `@layer reset, base, layout, components, utilities` で構成。色は `:root` のカスタムプロパティ
- 言語切り替え … `<html>` の `lang-en` クラスで `[lang="ja"]` / `[lang="en"]` を出し分け。localStorageに保存
- お問い合わせフォーム … サーバーを持たないので **mailto方式**。末尾のJSが送信を横取りし、
  入力内容を本文に組み立ててお客さんのメールアプリを開く。宛先はJSの `TO` に定数で持たせてある
  - **`<form>` に `action` を付けないこと。** 一度 `action="mailto:..."` を保険として
    書いたが、ブラウザが「安全でない送信先」と判定して**自動入力を無効化**してしまい、
    「このフォームは安全ではないため自動入力をオフにしました」の警告が出た。
    宛先がHTMLに露出してスパム業者に拾われる問題もあるため削除済み
- ヒーロー … 自動再生・無音・ループの背景動画。`prefers-reduced-motion` ではポスター画像のみ表示
  - **高さの単位に `dvh` を使わないこと。** `dvh` はスクロール中にブラウザのバーが出入りするたび
    値が変わるため、高さが伸び縮みして画面が震える(実機で発生を確認済み)。`svh` を使う
  - スマホのヒーローは `calc(100svh - var(--head-h))` で「ヘッダーを除いた画面いっぱい」。
    ヘッダーは `block-size: var(--head-h)` で高さを固定してあるので、この計算が崩れない
  - スマホ固定バーぶんの余白は `body` ではなく **各要素**に持たせる。
    `body` に下余白を付けるとフッターの下に白い帯が見えてしまう
    (ヒーローは padding-block-end に、フッターは緑をバーの下まで伸ばして対応)
  - **スマホでは動画の高さを `100lvh - var(--head-h)` に固定してある。**
    ヒーローの枠は `svh` 基準だが、実機ではバーが引っ込むと枠が伸び、つられて動画まで
    拡大して見える。動画だけ「バーが引っ込んだときの高さ(`lvh`)」に先に合わせておくことで、
    スクロール中に大きさが変わらないようにしている(はみ出しは `overflow: clip` で切る)。
    `max(100%, …)` で枠の高さを下限にしているのは、中身が多くて枠が伸びたときに
    下に隙間ができないようにするため
  - **`backdrop-filter`(すりガラス)をこのサイトで使わないこと。**
    再生中の動画に重なると、スクロールのたびに動画をぼかし直すことになり、
    スマホ実機で文字やボタンが引っかかる(実機で発生を確認済み)。
    ヘッダーとヒーローのバッジに入っていたが両方とも削除し、
    半透明の背景色だけで同じ見た目を出している
  - **ヒーロー内で `z-index` にマイナス値を使わないこと。**
    背景動画をマイナス階層に置くと、iPhone(Safari系)で動画が再生を始めた瞬間に、
    上に載っている文字が一瞬消える不具合が起きる(実機で発生を確認済み)。
    `.hero__media` を `z-index: 0` + `transform: translateZ(0)` で独立レイヤーに固定し、
    `.hero__inner` を `z-index: 1` に置くことで回避している

## デザインの方針

クライアントの希望は「白・緑・茶／植物系／ウッディすぎない／ナチュラルな沖縄・南国・白壁」。

- 背景は**クリーム色ではなく、やや冷たい白**(`--paper: #f6f6f2`)。テラコッタ系のアクセントは使わない
- 主役の色はロゴの濃い緑 `--pine`。**ヘッダーは白背景 + 緑のロゴ・文字**(下端に `--line` の罫線1本)。
  フッターは `--pine-deep` の帯にロゴを白抜きで置く
- **書体はサイト全体をセリフ体で統一**。和文も欧文も **Zen Old Mincho**(Google Fonts)1本でまかなう
  - ゴシック体を混ぜないこと。クライアントから「書体が多い」と指摘があり統一した経緯がある
  - 端末に入っている明朝体に頼ると Windows / iPhone / 海外端末で見た目がバラバラになり、
    海外端末では明朝体が無く崩れることがあるため、Webフォントを1つだけ読み込んでいる。
    読み込むウェイトは **400 / 500 / 700 のみ**。`Zen Old Mincho` に 300 は無いので指定しないこと
  - **数字も明朝体**。以前は数字(0-9 と ¥)だけ Segoe UI を切り出した `SiteNumerals` に
    差し替えていたが、これは端末ごとに数字がバラつくのを防ぐ目的だった。Webフォント導入で
    全端末が同じ字になり役目を終えたうえ、数字だけゴシックで浮いて見えるため削除済み
    (`assets/fonts/` フォルダごと削除)
- **`text-wrap: balance` / `pretty`、`word-break: auto-phrase` は使わないこと。**
  一度導入したが「右側に余白を残して早く改行される」とクライアントから指摘があり全て削除した。
  折り返しはブラウザ標準のまま(右端まで使って改行)にする
- **このページの「顔」はチネイザン3コースの `極 / 匠 / 和` の大きな漢字**。装飾を足すならここ以外を静かに保つ
  - カードの背景には店内の更紗の布(`pattern.webp`)を透かしとして敷いている。
    白地の画像を `mix-blend-mode: multiply` で重ね、`mask-image` で縁をぼかしている
- 売りたいのは **極(¥15,700)**。「いちばんのおすすめ」バッジと緑の枠で強調している。
  匠を「標準」「人気」と書かないこと(単価を下げる方向に効くため、クライアントの指示で削除済み)
- **金色(`--gold`)を使ってよいのは2ヶ所だけ**。極のカード(黒地)と、クチコミの★。
  ★は白いカードの上に載るため `--lamp` を少し混ぜて濃くしてある(そのままだと薄く飛ぶ)

### お客様の声(横に流れるクチコミ)

- **カードは1組だけ書く。** つなぎ目を消すための2組目は `index.html` 末尾のJSがコピーで作る。
  HTMLに2組書くと「増やすとき両方直す」必要が出て、直し忘れのもとになるため
- **クチコミの本文は実在のGoogleクチコミ**。文言を書き換えたり、創作で増やしたりしないこと
- **載せている内容は `docs/review.md` が正**。サイトに足す・減らすときは review.md も同じに保つ
  (review.md は `docs/` 配下なのでGitには入らない。手元のパソコンにだけある)
- 投稿者名はGoogleの表示名そのまま。カードのどこを押してもクチコミ一覧へ飛ぶ
- 速さは `--voices-speed`(カード1枚が通り過ぎる秒数)で調整する。
  枚数(`--voices-count`)はJSが数えて入れるので、カードを足しても速さは変わらない
- **既定は「指で横スワイプするリスト」**で、JSがコピーを作れたときだけ流れ始める(`.is-looping`)。
  JSが動かない環境でもカードが全部読めるようにするため
- 動きを減らす設定(`prefers-reduced-motion`)の人には流さず、スワイプするリストに戻る
- **効果を断定するようなクチコミは載せないこと。** 実在のクチコミでも、店が自サイトに
  載せると「店の表示」として扱われうる。「医療脱毛と同等の効果」と読める1件は削除済み

### 縦の左端を揃える

- **ヒーローの左右の余白は `.hero` ではなく `.hero__inner` に持たせること。**
  `.section` は「余白こみで `--wide` の箱」なので、`.hero` 側で余白を取ると
  「余白を引いた残りの中で `--wide`」になり、ヒーローの文字だけが余白1個ぶん左にズレる。
  両方とも余白を箱の内側に入れて、左端が縦一直線に通るようにしてある

### セクションの背景色

- **白(`--paper`)と薄グレー(`--paper-warm`)を必ず交互にすること。**
  同じ色が2つ続くと上下の余白(各 `--section-y`)が足し算され、
  画面の3分の1が何もない帯に見えてしまう(voices と therapist で実際に起きていた)。
  現在: about白 / menu薄 / hair白 / flow薄 / voices白 / therapist薄 / access白 / reserve緑 / faq白
- **予約(`.section--reserve`)だけは緑(`--pine-deep`)。**
  ページで唯一の目的が予約なので、ここだけ色を変えて「行動する場所」だと分かるようにしている。
  緑地の上では文字・罫線・ボタンが沈むため、`.section--reserve` 配下でまとめて上書きしてある
  (Instagram と送信ボタンは白地に緑文字へ差し替え)
  - **このセクションに白背景の部品を置くときは、文字色を必ず明示すること。**
    文字色は緑地に合わせて白に上書きされているので、指定を忘れると
    「白い部品の上に白い文字」になって読めなくなる
    (フォームの入力欄で実際に起きた。`.field :where(input, textarea)` に `color` を追加して解消)
- 背景を画面の端まで広げる仕組みは `box-shadow` + `clip-path`。`.section--tint` と同じ

### ナビゲーション

- ヘッダーのナビに **「ご予約」(`.nav-cta`)** を入れてある。パソコンではスマホの固定バーが消えるため、
  途中で予約したくなった人の行き先がこれしかない。消さないこと
- 項目が7つあるので、`.site-nav` の `gap` は `clamp()` で狭い幅では詰めてある
  (52rem = 832px 時点で約100pxの余裕がある)

### 写真の入れ方

- 料金表は `44rem` で止まるため、広い画面では右が大きく空く。
  `.menu-extra` と `.flow-wrap` で2列にして、右に店内写真を1枚ずつ置いて埋めている
- 写真はスマホでは `4/5`(背を低めに切る)、パソコンでは `9/16`(元の縦長のまま)に切り替えている。
  スマホで縦長のまま出すと写真1枚が画面を占領してしまうため
- 雰囲気写真の `alt` は空(`alt=""`)。すぐ横の見出しが内容を説明しているので、
  `.feature img` と同じく装飾扱いにしてある(日英2枚並べる必要もなくなる)

## ローカルでの確認

`index.html` をブラウザで直接開けば動きます。
スマホ幅を確認したいときは、`index.html` を iframe で読み込む確認用HTMLを一時的に作るのが手軽です(確認後は削除)。

## 素材について

- `assets/` … 公開用の軽量化済みファイル。写真は横1080pxのWebP、動画は720p・音なし・2MB以下
- `_src/` … 元データ(スマホ写真の原本・4K動画)。**200MB超あるので `.gitignore` 済み**
- 画像・動画の変換には ffmpeg を使用(コマンド例は `README.md`)

## 現在の未完了項目

- ヒーロー動画は**フリー素材の仮置き**。キシさんの実際の店内映像に差し替える
- 足湯・ドリンク・洗面所・外観の写真が未入手
- `assets/img/` に未使用の写真が10枚ある(candle / fire-lamp / rattan-lamp / hanging-pothos /
  pothos-shelf / anthurium / eucalyptus / menu-boards / print-cloth / certificates)。
  **certificates(認定証)は本名がはっきり写っている**ので、載せる前にキシさんへ確認が必要
- `CLAUDE.md` の「更紗の布を透かしに敷いている」という記述は、`styles.css` に該当する指定がなく
  `pattern.webp` も存在しない。実装するか記述を消すか決めること

日英で内容が変わる画像(キシさんの写真・決済一覧)は、同じ `src` の `<img>` を
`lang="ja"` / `lang="en"` の2つ並べて alt だけ出し分けている。片方だけ直さないこと。
