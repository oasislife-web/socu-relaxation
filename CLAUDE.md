# CLAUDE.md

このファイルは、このリポジトリで作業する際にClaude Code (claude.ai/code) に向けたガイドです。

## プロジェクト概要

**実在する店舗の受注案件です**(デモサイトではありません)。内容を勝手に創作しないでください。

- 店舗: 脱毛・リラク SOCU(福岡市博多区・博多駅徒歩5分)
- セラピスト: キシさん(男性1名)。全メニュー男性限定
- 主力: チネイザン(タイ古式の腹部トリートメント)。ほかにオイルトリートメント、メンズ脱毛
- 条件: 料金10万円(買い切り)／納期1週間

## このサイトのゴール

> 博多に来た国内・海外の観光客が、スマホで見つけて「ここなら安心して入れそう」と思い、
> そのままLINEかInstagramで予約できるサイト。

判断に迷ったらこの一文に立ち返ること。詳細は `docs/サイト構成案.md` を参照。

## 必ず守ること

- **事実は `docs/` が正**。料金・住所・営業時間は `docs/メニュー・料金.md` と `docs/店舗情報まとめ.md` から取る
- **電話番号は載せない**。電話予約を受け付けていないため(意図的な仕様)
- **お客様の声は実在のGoogleクチコミ**。作文して増やさない
- **全メニューが男性限定**。女性向けの表現を入れない
- 日本語と英語は必ずセットで直す(片方だけ直すと表示が食い違う)

## 構成

素のHTML/CSS + 小さなインラインJSのみ。ビルドシステム・パッケージマネージャー・テストはありません。

- `index.html` … 1ページ完結のランディングページ
- `styles.css` … `@layer reset, base, layout, components, utilities` で構成。色は `:root` のカスタムプロパティ
- 言語切り替え … `<html>` の `lang-en` クラスで `[lang="ja"]` / `[lang="en"]` を出し分け。localStorageに保存
- ヒーロー … 自動再生・無音・ループの背景動画。`prefers-reduced-motion` ではポスター画像のみ表示
  - **ヒーロー内で `z-index` にマイナス値を使わないこと。**
    背景動画をマイナス階層に置くと、iPhone(Safari系)で動画が再生を始めた瞬間に、
    上に載っている文字が一瞬消える不具合が起きる(実機で発生を確認済み)。
    `.hero__media` を `z-index: 0` + `transform: translateZ(0)` で独立レイヤーに固定し、
    `.hero__inner` を `z-index: 1` に置くことで回避している

## デザインの方針

クライアントの希望は「白・緑・茶／植物系／ウッディすぎない／ナチュラルな沖縄・南国・白壁」。

- 背景は**クリーム色ではなく、やや冷たい白**(`--paper: #f6f6f2`)。テラコッタ系のアクセントは使わない
- 主役の色はロゴの濃い緑 `--pine`。ヘッダーとフッターは `--pine-deep` の帯にロゴを白抜きで置く
- **書体はサイト全体をセリフ体で統一**(和文=明朝体、欧文=Georgia)。Webフォントは読み込まない
  - ゴシック体を混ぜないこと。クライアントから「書体が多い」と指摘があり統一した経緯がある
  - **数字(0-9 と ¥)だけは Segoe UI**。`@font-face` + `unicode-range` の `SiteNumerals` で
    数字のときだけ差し替えている。Segoe UIはWindows専用のため、他OSでは代替フォントになる
- **`text-wrap: balance` / `pretty`、`word-break: auto-phrase` は使わないこと。**
  一度導入したが「右側に余白を残して早く改行される」とクライアントから指摘があり全て削除した。
  折り返しはブラウザ標準のまま(右端まで使って改行)にする
- **このページの「顔」はチネイザン3コースの `極 / 匠 / 和` の大きな漢字**。装飾を足すならここ以外を静かに保つ
  - カードの背景には店内の更紗の布(`pattern.webp`)を透かしとして敷いている。
    白地の画像を `mix-blend-mode: multiply` で重ね、`mask-image` で縁をぼかしている
- 売りたいのは **極(¥15,700)**。「いちばんのおすすめ」バッジと緑の枠で強調している。
  匠を「標準」「人気」と書かないこと(単価を下げる方向に効くため、クライアントの指示で削除済み)

## ローカルでの確認

`index.html` をブラウザで直接開けば動きます。
スマホ幅を確認したいときは、`index.html` を iframe で読み込む確認用HTMLを一時的に作るのが手軽です(確認後は削除)。

## 素材について

- `assets/` … 公開用の軽量化済みファイル。写真は横1080pxのWebP、動画は720p・音なし・2MB以下
- `_src/` … 元データ(スマホ写真の原本・4K動画)。**200MB超あるので `.gitignore` 済み**
- 画像・動画の変換には ffmpeg を使用(コマンド例は `README.md`)

## 現在の未完了項目

- ヒーロー動画は**フリー素材の仮置き**。キシさんの実際の店内映像に差し替える
- お問い合わせフォームの `action` が空(送信されない)
- 足湯・ドリンク・洗面所・外観の写真が未入手
- 確認用公開のため `index.html` に `noindex` を入れている。本番ドメイン移行時に削除すること

日英で内容が変わる画像(キシさんの写真・決済一覧)は、同じ `src` の `<img>` を
`lang="ja"` / `lang="en"` の2つ並べて alt だけ出し分けている。片方だけ直さないこと。
